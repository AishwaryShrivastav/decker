name = "decker"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

[vars]
# OPENAI_API_KEY = "sk-..."  # optional server-side fallback; users supply their own via X-Api-Key
