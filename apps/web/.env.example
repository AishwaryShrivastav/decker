# No server-side API keys needed.
# Users provide their own OpenAI key via the extension settings (⚙ icon in the panel).
# The key is stored in chrome.storage.local and forwarded to the API as X-Api-Key.
